#include "../inc/game.h"

using namespace _16nar;

void Game::load_scene(const std::string& name) {

}

void Game::start_scene() {
	/*Clock clock;
	Time last_update = Time::Zero;
	while (game_window.isOpen())
	{
		last_update += clock.restart();
		while (last_update > time_per_frame)
		{
			last_update -= time_per_frame;
			processEvents();
			update(time_per_frame);
		}
		render();
	}*/
}

void Game::load_resource(ResourceType type, const std::string& path) {

}

void Game::free_resource(const std::string& path) {

}
